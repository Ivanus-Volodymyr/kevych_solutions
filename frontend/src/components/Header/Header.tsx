import React, {FC} from 'react';

import './Header.css';

const Header: FC = () => {
    return (
        <div className={"header"}>
            <h2>Header</h2>
        </div>
    );
};

export { Header };
